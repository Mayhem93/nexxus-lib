{
  "type": "object",
  "properties": {
    "level": {
      "type": "string",
      "enum": ["debug", "info", "warn", "error", "critical", "alert", "emerg"]
    },
    "logType": {
      "type": "string",
      "enum": ["text", "json"]
    },
    "timestamps": {
      "type": "boolean"
    },
    "colors": {
      "type": "boolean"
    }
  },
  "required": [
    "level",
    "logType"
  ]
}
